import { CodeSurfer } from 'mdx-deck-code-surfer';
import { Notes, Appear } from 'mdx-deck';
import BlankTemplate from '../wizeline-theme/WBlankTemplate';
import Codepen from './components/Codepen';
import Counter from './components/Counter';

<!-- slides code starts here -->

## Wizeline mdx-deck theme

# Features

---

## This is a regular slide

But sometimes you'll want to use...

---

export default BlankTemplate;

## The BlankTemplate

You just need to import it

```
import BlankTemplate from '../wizeline-theme/WBlankTemplate';
```

and add this line to your slide

```
export default BlankTemplate;
```

### Useful when using images or large content

---

### This is a custom React component

<Counter/>
<Notes>
  Sadly any interactive component like this one
  won't change it's state in both windows,
  any demo should be worked on the "normal mode" window.
</Notes>

---

<!-- ugly code on purpose -->

```js
import React from 'react';
import styled from 'styled-components';

const Title = styled.h1`
  font-size: 1.5em;
  text-align: center;
  color: palevioletred;
`;

const Description = styled.p([], {
  color: 'purple'
});

export default class Counter extends React.Component {
  state = { counter: 0 };
  handleClick = () => {
    this.setState({ counter: this.state.counter + 1 });
  };

  render() {
    return (
      <div>
        <Title>Counter</Title>
        <Description>I'm using styled components</Description>

```

---

export default BlankTemplate;

<CodeSurfer
  title="Custom component"
  code={require('!raw-loader!./components/Counter.js')}
  lang="js"
  showNumbers={true}
  dark={false}
  steps={[
    { range: [1, 2], notes: 'This is your regular react component' },
    { range: [4, 8], notes: 'styled components' },
    { range: [14, 18], notes: 'handle props' },
    { range: [20, 29], notes: 'render function' }
  ]}
/>

---

export default BlankTemplate;

<CodeSurfer
  title="<CodeSurfer/>"
  code={require("!raw-loader!./snippets/css-snippet-example.css")}
  lang="css"
  showNumbers={false}
  dark={false}
  steps={[
    { lines: [6], notes: 'You can zoom in'},
    { lines: [3, 11, 18], notes: 'select many lines'},
    { range: [9,15], notes: 'or even a range of lines' },
    { tokens: { 17: [1, 6] }, notes: 'also specific tokens' }
  ]}
/>
---

export default BlankTemplate;

<CodeSurfer
  title="How to use it?"
  code={require('!raw-loader!./wize-theme.mdx')}
  lang="js"
  showNumbers={true}
  dark={false}
  steps={[
    { lines: [1], notes: 'Import it' },
    { range: [121, 131], notes: 'and add props' }
  ]}
/>

---

export default BlankTemplate;

<Codepen
  title="<Codepen/>"
  label="Open"
  url="https://codepen.io/ederdiaz/pen/NBPWZp"/>
<Notes>
This is a working codepen,
be careful when clicking the sides as you might 
change the slide accidentally.

Also it won't change it's state in both windows,
any demo should be worked on the "normal mode" window.

</Notes>

---

export default BlankTemplate;

<CodeSurfer
  title="How to use it?"
  code={require('!raw-loader!./wize-theme.mdx')}
  lang="js"
  showNumbers={false}
  dark={false}
  steps={[
    { lines: [4], notes: 'Import it' },
    { range: [137, 141], notes: 'and set codepen url' }
  ]}
/>
